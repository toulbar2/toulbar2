######################################
# personal properties for -D Option
######################################

set(PROBABILITY "DOUBLE_PROB")

if(LONG_PROBABILITY)
  set(PROBABILITY "LONGDOUBLE_PROB")
ENDIF(LONG_PROBABILITY)

if(QUAD_PROBABILITY)
  set(PROBABILITY "QUAD_PROB")
ENDIF(QUAD_PROBABILITY)

if(LONG_COSTS)
  SET(COST LONGLONG_COST)
ELSE(LONG_COSTS)
  set(COST INT_COST)
ENDIF(LONG_COSTS)

IF(WIN64)
  #add_definitions(-DWIN32 -D_DEBUG -D_WINDOWS -D_USRDLL )
  #add_definitions(-D__MINGW32__)

  IF(TOULBAR2)
    SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -DWCSPFORMATONLY -DLONGLONG_COST -DDOUBLE_PROB -D_WIN32 -DWIN32 -D_DEBUG -D_WINDOWS -DWINDOWS")
    set_property(
      TARGET toulbar2${EXE}
      PROPERTY COMPILE_DEFINITIONS WCSPFORMATONLY LONGLONG_COST DOUBLE_PROB _WIN32 WIN32 _DEBUG _WINDOWS WINDOWS)
  ENDIF(TOULBAR2)
  
  IF(MENDELSOFT)
    SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -DWCSPFORMATONLY -DINT_COST -DDOUBLE_PROB -DMENDELSOFT -D_WIN32 -DWIN32 -D_DEBUG -D_WINDOWS -DWINDOWS")
    set_property(
      TARGET mendelsoft${EXE}
      PROPERTY COMPILE_DEFINITIONS WCSPFORMATONLY INT_COST DOUBLE_PROB MENDELSOFT _WIN32 WIN32 _DEBUG _WINDOWS WINDOWS)
  ENDIF(MENDELSOFT)

  IF(LIBTB2)
    SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -DLONGLONG_COST -DDOUBLE_PROB -D_WIN32 -DWIN32 -D_DEBUG -D_WINDOWS -DWINDOWS")
    set_property(
      TARGET tb2
      PROPERTY COMPILE_DEFINITIONS LONGLONG_COST DOUBLE_PROB _WIN32 WIN32 _DEBUG _WINDOWS WINDOWS)
  ENDIF(LIBTB2)
  
ELSE(WIN64)
  
  IF(TOULBAR2)
    set_property(
      TARGET toulbar2
      PROPERTY COMPILE_DEFINITIONS WCSPFORMATONLY ${COST} ${XMLFLAG} LINUX ${boostflag} ${mpiflag} ${PROBABILITY})
    set_property(
      TARGET tb2-archive
      PROPERTY COMPILE_DEFINITIONS ${COST} ${XMLFLAG} LINUX ${boostflag} ${mpiflag} ${PROBABILITY})
    set_property(
      TARGET tb2-objects
      PROPERTY COMPILE_DEFINITIONS ${COST} ${XMLFLAG} LINUX ${boostflag} ${mpiflag} ${PROBABILITY})
  ENDIF(TOULBAR2)
  
  IF(MENDELSOFT)
    set_property(
      TARGET mendelsoft
      PROPERTY COMPILE_DEFINITIONS WCSPFORMATONLY ${COST} MENDELSOFT LINUX ${boostflag} ${mpiflag} ${PROBABILITY})
  ENDIF(MENDELSOFT)
  
  IF(LIBTB2)
    set_property(
      TARGET tb2-PIC-objects
      PROPERTY COMPILE_DEFINITIONS ${COST} ${XMLFLAG} LINUX ${boostflag} ${mpiflag} ${PROBABILITY})
    set_property(
      TARGET tb2
      PROPERTY COMPILE_DEFINITIONS ${COST} ${XMLFLAG} LINUX ${boostflag} ${mpiflag} ${PROBABILITY})
    IF(PYTB2)
      set_property(
	TARGET pytb2
	PROPERTY COMPILE_DEFINITIONS ${COST} ${XMLFLAG} LINUX ${boostflag} ${mpiflag} ${PROBABILITY})
    ENDIF(PYTB2)
  ENDIF(LIBTB2)
  
  if(ILOG)
    set_property(
      TARGET iloglue
      PROPERTY COMPILE_DEFINITIONS WCSPFORMATONLY INT_COST ILOGLUE IL_STD LINUX ${PROBABILITY})
  ENDIF(ILOG)
  
ENDIF(WIN64)
